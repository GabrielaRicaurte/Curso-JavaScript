/* 
    ======================================
    ==== COMPARADOR MAYOR O MENOR QUE ====
    ======================================
*/

const dinero = 500;
const totalAPagar = 300;

if( dinero >= totalAPagar ) { /* >= Mayor o igual a, <= Menor o igual a... */
    console.log( 'Si podemos pagar' );
} else {
    console.log( 'Fondos insufucientes' );
}